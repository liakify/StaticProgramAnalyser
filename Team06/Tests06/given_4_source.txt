procedure A {
    read p;
    q = p + 4;
    while ( q != 4 ) {
        print q;
        q = q - 1; } }

procedure B {
    s = B + x + 10;
    z = B + x / y * z + 10;
    y = 26;
    while ( s == 4 ) {
        y = s + B;
        s = x;
        call A;
        if ( A == ( s - 4 ) ) then {
            read x;
            x = A + 4;
            print A; }
        else {
            while ( q > s ) {
                A = B + x + 10 / y * z;
                call C; } } }
    y = 7; }

procedure C {
    if ( ( A - q ) == 10 ) then {
        print A;
        call A; }
    else {
        B = B + x; }
    read B; }